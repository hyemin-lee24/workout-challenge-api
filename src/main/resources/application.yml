spring:
  application:
    name: workout-challenge-api
  datasource:
    url: jdbc:postgresql://localhost:5432/public1
    username: postgres1
    password: postgres1
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true